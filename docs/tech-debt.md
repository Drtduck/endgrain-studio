# Технический долг и бэклог

Список того, что решено сделать позже. Ничего отсюда не берётся в работу без явного
согласия Станислава. Порядок внутри файла - порядок появления, а не приоритет.

Заведён 12 августа 2026, во время подготовки конкурсной сдачи (Вайбатон №1).

---

## 1. Cookie compliance, США

Баннер согласия и политика cookies под требования штатов (CCPA/CPRA в Калифорнии,
плюс аналогичные законы Вирджинии, Колорадо, Коннектикута, Юты и далее). Отличие от
Европы принципиальное: в США модель opt-out, нужна ссылка «Do Not Sell or Share My
Personal Information» и уважение сигнала Global Privacy Control, а не предварительный
запрос согласия.

Что решить: какие категории cookies у нас вообще есть (сейчас это сессия Supabase и,
после пункта 9, аналитика), нужна ли отдельная страница политики, где хранить выбор
пользователя и как не сломать им аналитику.

## 2. Ресерч по compliance для России

Отдельный большой ресерч, до кода. Тема: 152-ФЗ о персональных данных и смежные
требования к сайтам - согласие на обработку персданных в формах и при регистрации,
отдельное согласие на cookies, политика обработки персданных как обязательный
документ, уведомление Роскомнадзора, требование локализации баз персданных
российских граждан на территории РФ.

Главный нерешённый вопрос - как определять таких пользователей. По IP через
`x-vercel-ip-country` (заголовок у нас уже пробрасывается в `x-egs-country`), но VPN,
роуминг и корпоративные прокси делают эту оценку приблизительной. Надо продумать
комбинацию сигналов: страна по IP, язык интерфейса, выбранная локаль, домен почты,
и что делать при конфликте. И отдельно: где проходит граница разумного для проекта
нашего размера, потому что полное соблюдение локализации баз означает отдельную
инфраструктуру в РФ.

## 3. Дешёвый fallback для AI-генераций через fal.ai

Сейчас генерация изображений завязана на Gemini. Нужен запасной и очень дешёвый
путь через fal.ai: подобрать самую дешёвую пригодную модель, замерить реальную
стоимость кадра.

Продуктовая логика: бесплатному пользователю даём три пробные генерации на дешёвой
модели, дальше показываем честное предложение купить Pro, где генерация идёт на
хорошей модели. Счётчик бесплатных попыток надо привязать не только к браузеру,
иначе он обнуляется в режиме инкогнито.

## 4. Непрямоугольные узоры и формы досок

Сегодня движок умеет только правильную прямоугольную сетку торцов. Нужна поддержка
мозаики: треугольники, ромбы, произвольные многоугольники, а также доски неправильной
формы (скруглённые углы, силуэты, фигурные торцы).

Первым шагом - не код, а ресерч методологии и готовых наработок: как это считают в
столярке (end grain mosaic, чертежи распила под углом), и что уже есть на GitHub и в
академических работах по замощениям и раскрою. Изобретать с нуля только если ничего
пригодного не найдётся. Ключевая сложность именно в нашей части: любой узор должен
разложиться в выполнимую последовательность склеек и резов с учётом пропила и
припусков, иначе получится рисовалка, а не производственный инструмент.

## 5. Pro-пакет «подготовка к продаже»

Расширение Pro до полного набора для тех, кто продаёт готовые доски на Amazon и Etsy.
Содержимое: генерация описания товара с реальными размерами из проекта, заголовок,
буллеты, ключевые слова, категории, материалы, уход - то есть готовый набор полей под
карточку конкретной площадки. Для попадания в тон и в ключевые слова нужен ресерч или
скрейпинг живых карточек Amazon и Etsy, тут отдельно проверить правила площадок.

Плюс медиа: фотографии товара (уже есть во вкладке «Промо») и короткое видео через
дешёвую модель на fal.ai.

Монетизация видео отдельная от Pro: $2 за генерацию 5 секунд, продаём блоками по $10,
скидка растёт от объёма пополнения. Значит нужен кошелёк с балансом внутри аккаунта,
а не только подписка: отдельная таблица, история списаний и пополнений через Stripe.

## 6. Социальная часть: публикация проектов, лайки, платные проекты

Залогиненный пользователь может опубликовать свой проект в общую галерею. Другие
видят его, ставят лайки и сохраняют себе. Автор при публикации выбирает: отдать
бесплатно или назначить цену. Лайк и просмотр бесплатны всегда, а вот повторить
проект у себя (открыть в студии, получить схему распила и размеры) можно только после
покупки, если автор поставил цену.

Отсюда тянется: выплаты авторам, доля площадки, модерация, жалобы, права на узор.
На лендинге под это отдельный блок с живой галереей популярного.

## 7. Блог

Отдельный раздел страниц под регулярные статьи. Нужен на своём домене, не на внешней
платформе, ради пункта 8. Определиться с хранением текстов (MDX в репозитории против
записей в базе), лентой, тегами, RSS и картинками.

## 8. SEO и GEO оптимизация

Посмотреть актуальные best practice и сделать по-нормальному: разметка Schema.org,
sitemap и robots, канонические адреса, Open Graph, скорость и Core Web Vitals,
внутренняя перелинковка. Отдельно GEO, то есть оптимизация под ответы ИИ-поисковиков:
llms.txt, цитируемость страниц, структура ответов на вопросы. В `~/.claude/skills`
для этого уже есть готовый набор скиллов, использовать их.

## 9. Google Analytics

Подключить аналитику как следует, а не «вставили счётчик»: GA4, события по ключевым
действиям (создал проект, экспортировал PDF, открыл тарифы, начал оплату, оплатил),
воронка, связка с consent-режимом из пунктов 1 и 2, чтобы счётчик не стрелял до
согласия там, где оно требуется.

## 10. Agent-ready SaaS и тариф Developer

Вишенка. Большой ресерч: как SaaS-продукты сейчас переводят себя в состояние
«готов к работе с ИИ-агентами», какие есть best practice и готовые решения. Цель -
чтобы всё, что человек делает мышкой на фронте, можно было сделать через MCP-сервер
и CLI: создать проект, поменять узор, посчитать распил, получить PDF, опубликовать
в галерею.

Это отдельная большая фича и отдельный платный тариф Developer со своим лимитом
вызовов и ключами API.

---

Дополнено 13 августа 2026, вечер (третий заход, пункты 1-10 первых двух волн
закрыты PR #22 и #24). Ничего отсюда не берётся в работу без явного согласия
Станислава. Скриншоты из постановки в чат не доехали (только метки Image #N),
при взятии в работу сверить детали на живом проде.

## 11. Страница профиля

Полноценная страница профиля пользователя. Первым шагом brainstorm: что кладут
туда по best practice у SaaS (аватар, имя, соцсети, смена почты/пароля, удаление
аккаунта, настройки уведомлений, публичный профиль автора для галереи, что ещё).
Дизайн-решение через Opus-панель, потом код.

**Сделано** (14 августа 2026): хаб `/account` (публичный профиль, смена почты и
пароля с учётом google-identity, уведомления, опасная зона с удалением аккаунта
через подтверждение почтой), публичная страница автора `/u/[id]` с сеткой
опубликованных работ, показ автора (`AuthorLine`) в карточках галереи и на
странице проекта. Таблица `public.profiles` с RLS и column-grants - миграция
`20260814100000_profiles.sql`.

## 12. Неработающая кнопка (Image #10)

По контексту: кнопка на одной из новых поверхностей последних волн (кандидаты
в порядке вероятности: меню профиля AccountMenu из PR #23, кнопки на
/account/api, «Опубликовать в галерею», кнопка врезки подписки в статье).
Первый шаг: прод-агентом прокликать все кнопки новых экранов и найти мёртвую,
затем починить.

**Сделано** (14 августа 2026, PR #31): прод обойдён дважды (гость и залогиненный),
мёртвых кнопок не найдено; «мёртвым» оказался задизейбленный «Купить - скоро»
из пункта 17, он заменён живой покупкой. Попутно починены двойная шапка
на /gallery и отсутствие фидбека у «Copy link» в Board settings.

## 13. Cookie-баннер не должен мешать ни одной странице

Баннер согласия перекрывает контент/элементы на некоторых страницах (скрин
Image #11). Правило: баннер никогда не должен мешать работе ни одной страницы.
Починить позиционирование/поведение и прогнать проверку по ВСЕМ страницам обоих
доменов (десктоп + мобайл), включая студию, галерею, блог, правовые, тарифы.

**Сделано** (14 августа 2026, PR #31): баннер стал компактной плавающей карточкой
(десктоп: слева внизу max-w-md; мобайл: ниже 120px), pointer-events только
на карточке, контент под ней кликается. «Настройки cookie» в футере повторно
открывают баннер (reopen без сброса согласия). Проверено e2e и на проде,
десктоп 1440x900 и мобайл 390x844.

## 14. Протестировать серию промо-фото для магазина

Вкладка «Промо», серия фото: везде выводится «Заглушка». Прогнать живую
генерацию серии (ключ Gemini на проде есть), результат сложить отдельной папкой
на рабочий стол Станислава. Разобраться, почему заглушки: демо-режим, квота или
баг.

**Сделано** (14 августа 2026, PR #31): живая генерация на проде работает через
fal (кадр в ~/Desktop/Endgrain-promo-2026-08-14/). Причина «заглушек»: free-тир
режет серию до одной генерации за вызов, а UI позволял выбрать четыре кадра -
теперь стартовый выбор честный. GEMINI_API_KEY на проде задан, но prepay-баланса
нет, поэтому работает фолбэк fal; после пополнения баланса Gemini включится сам.

## 15. Навигация «назад» выкидывает залогиненного на логин

Если из приложения нажать несколько раз «назад», залогиненный человек попадает
на страницу логина, а то и на google-авторизацию. Сначала brainstorm: как
должна работать история навигации после входа (replace вместо push на
auth-редиректах, guard на /login для авторизованных с редиректом в студию,
чистка OAuth-шагов из истории), потом фикс.

**Сделано** (14 августа 2026, PR #31, дизайн через Opus-панель): guard
залогиненных на /login и /register в decideAccess (redirect в next или '/'),
hardReplace вместо hardNavigate после входа, OAuth через skipBrowserRedirect
с заменой записи истории. /reset-password и /forgot-password сознательно
вне guard (recovery-сессия). Проверено юнитами, e2e и руками на проде.

## 16. PDF и тарифная витрина

PDF рендерится стрёмно. Оставить преимущество белого фона (не тратить краску
при печати), но сделать нормальное выравнивание, рамки, логотип, адрес сайта.

Тарифная витрина /pricing (по контексту скринов Image #12-#15):
- Карточка бесплатного тарифа: пункт «PDF-инструкция без подписи» выпилить,
  вместо него перечислить AI-функции (пробные генерации, карточка товара).
- Разовый оффер за $19: единая цена one-time покупки (по контексту - разовый
  платный пакет рядом с подпиской; сверить с текущей карточкой, где цена
  отсутствует или другая).
- Pro: вместо двух кнопок ($9/мес и $90/год) одна кнопка «от $7.50/мес»
  (90/12), ведёт сразу в Stripe Checkout с выбором $9/мес или $90/год,
  по умолчанию выбран год. Скидку показать на форме Stripe максимально
  маркетингово (promotion codes / custom_text / отображение «2 месяца в
  подарок» - что позволят доки Checkout).
- API/Developer: одна кнопка $20/мес, при оплате за год 2 месяца в подарок:
  $200/год (эффективные $16.67/мес, на кнопке «от $16.67/мес»).

**Сделано** (14 августа 2026, PR #31): PDF с колонтитулами (логотип, адрес
сайта, номера страниц, линейки, ровная таблица), белый фон сохранён. Витрина:
4 карточки, у Free AI-пункты вместо PDF-строки, разовый оффер оформлен как
«Пропуск» $19 (Pro на 90 дней, mode=payment, таблица pro_passes), у Pro одна
кнопка «от $7.50/мес» через Stripe Subscription Upsells (выбор месяц/год на
форме Checkout), Developer $20/мес / $200/год через subscriptions.product='api'
и set_api_tier. Для активации нужны ручные шаги в Stripe Dashboard: price ids,
upsells, display per month - инструкция в ~/Desktop/Endgrain/todo/tech-debt-wave-3.html.

**Снято с продажи** (15 августа 2026, docs/specs/pricing-wallet.md раздел 1):
продукт «Пропуск» больше не продаётся - карточка и путь покупки удалены с
витрины, `STRIPE_PRICE_PASS`/`hasPassPrice` убраны из кода. Уже купленные
пропуска дорабатывают срок как есть: таблица `pro_passes`, `grant_pro_pass`,
`ProReason='pass'` и ветка вебхука `payment.kind === 'pro_pass'` остаются
минимум до 01.11.2026.

## 17. Недоделанный флоу оплаты (Image #16)

По контексту: видимый пользователю недоделанный участок оплаты. Главные
кандидаты: покупка платного проекта в галерее (кнопка «Купить - скоро»,
схема БД и защита узора готовы, включение по спеке commerce-social) и/или
пополнение кошелька. Допилить полный цикл на тестовом аккаунте Stripe
(тестовые карты, вебхук), прогнать e2e, потом переключить на боевые ключи.

**Сделано** (14 августа 2026, PR #31): покупка платного проекта из галереи
по спеке commerce-social фаза 2: createPurchaseCheckoutAction (mode=payment,
kind=gallery_purchase), идемпотентная запись project_purchases в вебхуке,
покупателю открывается design. Кошелёк работал и раньше. Выплаты авторам
и Stripe Connect отложены по спеке. Переключение на боевые ключи Stripe -
вместе с ручными шагами пункта 16.

## 18. Себестоимость кадра AI: сверять раз в квартал

`AI_FRAME_COST_CENTS` (`lib/ai/cost.ts`, дефолт 8 центов) - число, от которого
посчитана наценка x2.5 в пакетах кадров (`lib/ai/packs.ts`). Провайдер (fal.ai)
может менять тариф модели без предупреждения, и число в env может разойтись с
реальным счётом. Раз в квартал сверять `AI_FRAME_COST_CENTS` со счетами fal.ai
и поправить значение в Vercel env, если себестоимость сдвинулась заметно.

## 19. Частичный успех генерации кадрами: не различается

Раздел 5.6 спеки docs/specs/pricing-wallet.md, последний абзац: если провайдер
отдал часть серии (например, 2 кадра из 4 запрошенных), `consume_ai_units`
резервирует и списывает стоимость целиком, а `release_ai_units` возвращает
резерв только при полном провале серии. Частично успешная генерация сейчас
не возвращает разницу человеку. Это осознанный долг, зафиксированный при
реализации кадров (волна C спеки pricing-wallet.md, 15 августа 2026): нужен
дизайн частичного возврата (какая часть резерва свободна, кадры или деньги)
до того, как это станет кодом.

<<<<<<< Updated upstream
## 20. Мерч-флоу: спроектирован, реализован, отложен целиком

Решение Стаса 15 августа 2026: мерч в техдолг, секция «Мерч с этим узором»
скрыта из вкладки «Промо» (PR #48), потому что старая кнопка «Открыть в
Printful» вела в кабинет Printful, которого у покупателя нет, а новая касса
не доведена до прода.

Что уже есть и где лежит:
- Спека docs/specs/merch-orders.md (813 строк): «Купить» -> Stripe Checkout
  со сбором адреса -> вебхук создаёт draft-заказ в Printful (confirm по env)
  -> страница «Мои заказы». Цена round99(себестоимость x MERCH_MARGIN +
  доставка), доставка вшита, RU/BY исключены.
- Полная реализация всех 7 этапов в ветке Drtduck/merch-orders (закрытый
  PR #47, 42 файла, 2518 тестов зелёные на момент ветки). Последний коммит
  a9c12d6 - частичные фиксы opus-ревью (агент остановлен на середине).
- Opus-ревью в PR #47: блокеров нет, 6 major (захват гонки вебхуков, узкое
  распознавание «order already exists», литерал вместо printful_order_id,
  неограниченный документ на серверный рендер 4000px, неподключённая уборка
  bucket, полноразмерный print как миниатюра) и 7 minor. Часть внесена в
  a9c12d6, остальное чинить перед мержем.
- Прод-инфраструктура уже стоит и ничему не мешает: миграции merch_orders
  и bucket merch-prints применены на боевой Supabase, env MERCH_ENABLED /
  MERCH_MARGIN / PRINTFUL_CONFIRM_ORDERS заведены (код main их не читает).
  Миграция merch_orders_bump_attempts лежит в ветке и НЕ применена.
- Снимок каталога сверен по живому Printful 15.08.2026: variant id размеров
  футболки подтверждены (S/M/L/XL = 4011/4012/4013/4014), себестоимость и
  худшие ставки доставки вписаны в lib/merch/catalog.ts ветки.

Для возврата: реанимировать ветку от свежего main, закрыть остатки находок
ревью, применить миграцию bump_attempts, вернуть секцию в PromoPanel, снять
skip с e2e/merch.spec.ts, прогнать живую покупку тестовой картой Stripe
(заказ уйдёт драфтом, Printful не бильнёт) и только потом MERCH_ENABLED.
=======
## 20. Security-хардеринг денежного слоя (defense-in-depth, нужна миграция прод-БД)

Аудит безопасности 15 августа 2026 (пять Opus-агентов, отчёт на рабочем столе:
`~/Desktop/Endgrain/todo/security-audit-2026-08-15.html`). Реально
эксплуатируемые дыры закрыты в PR #49 чистым серверным кодом: ключ
идемпотентности списания больше не приходит с клиента (`generateListingAction`,
`generateVideoAction` теперь генерируют ref на сервере), плюс open redirect в
auth-callback через `/\evil.com` и timing-safe сравнение `CRON_SECRET`.

Ниже три пункта, которые осознанно НЕ вошли в PR #49: каждый требует DDL по
живой денежной прод-базе, а такое хочется катить отдельным заходом с обкаткой
на Supabase-ветке, а не мешать с обратимым app-кодом. После серверного ref
межаккаунтный вектор (первые два пункта) через эти actions уже недостижим, так
что это hardening поверх уже закрытой дыры, а не открытая рана.

**20.1. Скоуп replay-выборки по `user_id`.** В `consume_ai_units`
(`supabase/migrations/20260815160000_promo_fixes.sql`) и `wallet_spend`
(`supabase/migrations/20260813110000_wallet.sql`) ветка идемпотентности ищет
строку списания только по `(kind, ref)`, без `user_id`. Уникальные индексы
`ai_credit_tx_kind_ref_idx` и `wallet_tx_kind_ref_idx` тоже глобальные по
`(kind, ref)`. Сделать: добавить `and user_id = p_user_id` в replay-SELECT
(`consume_ai_units`), пересоздать оба индекса как `(user_id, kind, ref)`, и не
считать replay строку с `released = true`. Тогда чужой/утёкший ref в принципе
не может открыть списание другому аккаунту, независимо от того, как
формируется ref в приложении.

**20.2. `bump_save_count` доступна напрямую (накрутка счётчиков галереи).**
`supabase/migrations/20260813100000_gallery.sql:219-237`: функция
`security definer`, `grant execute ... to authenticated`, инкрементит
`saves_count` у любой строки без проверки владения и видимости. Та же миграция
специально забирает у `authenticated` право писать в счётчики, а эта RPC его
возвращает через чёрный ход, вызываемый прямым PostgREST-запросом. Сделать:
либо перенести инкремент на сервер под service-role (путь копирования уже идёт
через server action `copyPublishedAction`, `app/actions/gallery.ts:250`), либо
`revoke execute ... from authenticated`, либо оставить функцию, но проверить
внутри, что `auth.uid()` реально скопировал `p_id` и публикация видима (как в
`published_project_design`). Импакт низкий (vanity-счётчик, не деньги), поэтому
и отложено, но закрывать надо на стороне БД - app-код один эту дыру не закроет.

**20.3. Гонка в `retryPromoShotAction` даёт бесплатный кадр.**
`app/actions/promo.ts:242-258`: статус меняется на `queued` одним update, а
`retries = retries + 1` - отдельным update строкой ниже. В окне между ними
`POST /api/promo/shot` захватывает кадр со старым `retries`, то есть с ref
предыдущей попытки (за которую деньги уже вернулись через `release_ai_units`),
и `consume_ai_units` отвечает replay: кадр рисуется провайдером бесплатно.
Правильный фикс - единый атомарный `update ... set status='queued',
retries = retries + 1 where status='failed' and retries < 3 returning *`, а это
выразимо только SQL-RPC (PostgREST не умеет `retries + 1` вместе со сменой
статуса без потери гварда от двойного клика). Поэтому нужен новый RPC
`retry_promo_shot(p_shot_id, p_user_id)` и правка `promo.ts` под него.
Ограничено `retries < 3` и требует точного попадания в окно, отсюда Medium.
>>>>>>> Stashed changes

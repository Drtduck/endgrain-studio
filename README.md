# Endgrain Studio

Генератор узоров для торцевых разделочных досок. Проект для Вайбатона №1 (срок - 7 дней, старт 10 августа 2026).

Цель - не рисовалка, а рабочий инструмент столяра: придумать узор, увидеть готовую доску, понять схему распила и переклеек, посчитать размеры деталей, материал и отходы, сохранить проект и распечатать инструкцию для мастерской.

## Минимально рабочая версия

- редактор узора;
- несколько пород или цветов древесины;
- поворот и отражение элементов;
- настройка размеров доски;
- сохранение проекта;
- экспорт изображения узора (готово).

## Что добавляет очков

Генеративные и случайные узоры, библиотека шаблонов, интерактивное 3D-превью, учёт толщины пропила, карта раскроя в CSV, пошаговая схема переклеек в PDF, расчёт древесины и отходов, расчёт себестоимости, миллиметры и дюймы, PDF-инструкция с кириллическим шрифтом, проверка физической реализуемости, оптимизация раскроя, импорт изображения и превращение его в узор, мм и дюймы в экспорте.

## Критерии оценки

Функциональность (можно ли изготовить настоящую доску), удобство для человека, который не писал код, качество и разнообразие генерации, техническая реализация, общий вайб. Работающий продукт важнее списка функций в README.

## Что сдаём

Ссылку на работающий продукт, ссылку на репозиторий, короткое видео или GIF с демонстрацией, несколько примеров досок и краткое объяснение, как изготовить доску по полученному проекту.

## Статус

Инфраструктура заведена: приватный репозиторий и проект в Orca. Стек и архитектура пока не выбраны.

## Прод

https://endgrain-studio.vercel.app

Автодеплой при пуше в `main` подключён через Vercel Git Integration (репозиторий `Drtduck/endgrain-studio`).

## Переменные окружения

Аккаунт и облачные проекты необязательны: без переменных Supabase студия работает как раньше, локально, без кнопки аккаунта и вкладки «Мои проекты».

| Переменная | Назначение |
| --- | --- |
| `NEXT_PUBLIC_SUPABASE_URL` | URL проекта Supabase. Обязательна для аккаунта и облачных проектов, инлайнится в клиентский бандл. |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Anon-ключ Supabase. Обязателен вместе с `NEXT_PUBLIC_SUPABASE_URL`, все операции идут под ним через RLS. |
| `SUPABASE_SERVICE_ROLE_KEY` | Нужен вебхуку Stripe, чтобы писать в `subscriptions` в обход RLS (`lib/supabase/admin.ts`), и загрузке макета мерча во временный bucket `promo-mockups` (`lib/promo/storage.ts`). Без него вебхук отвечает 503, а мокапы мерча остаются локальными набросками. |
| `GEMINI_API_KEY` | Ключ Gemini для серии промо-кадров и разбора референса. Без него вкладка «Промо» рисует локальные заглушки, честно предупреждая об этом. |
| `FAL_KEY` | Ключ fal.ai: дешёвый провайдер для Pro-фолбэка и мотор бесплатного пробного тира генераций. |
| `FREE_TRIAL_SECRET` | Секрет подписи гостевой cookie `egs_ft` и хеша IP в бесплатном тире. Без него пробный тир для гостей выключен целиком. |
| `PRINTFUL_API_KEY` | Ключ Printful для настоящих мокапов мерча (`lib/promo/printful.ts`). Без него вкладка «Мерч» показывает только локальные схематичные наброски. |
| `PRINTFUL_STORE_ID` | Id магазина Printful, нужен генератору мокапов вместе с `PRINTFUL_API_KEY` (токен уровня магазина заголовка не требует, тогда можно оставить пустым). Заводится в кабинете Printful, через API его не создать. Без него запрос падает с понятной подписью в интерфейсе, а не тихо. |
| `STRIPE_SECRET_KEY` | Секретный ключ Stripe (`sk_live_` или `sk_test_`). Серверная, в клиентский бандл не попадает. |
| `STRIPE_WEBHOOK_SECRET` | Секрет эндпоинта вебхука (`whsec_`). Серверная. |
| `NEXT_PUBLIC_STRIPE_PRICE_MONTHLY` | Id месячной цены (`price_...`). Публичная, инлайнится в бандл: id цены не секрет. |
| `NEXT_PUBLIC_STRIPE_PRICE_YEARLY` | Id годовой цены. Публичная. |
| `NEXT_PUBLIC_STRIPE_PORTAL_URL` | Ссылка на hosted Customer Portal. Необязательна: без неё пункт «Управлять подпиской» не рендерится. |
| `E2E_STRIPE` | Флаг `1` включает группу e2e с живыми ключами Stripe. По умолчанию и в CI пропускается. |
| `E2E_AUTH` | Флаг `1` включает `e2e/auth.spec.ts` локально. По умолчанию (без флага) и в CI спека пропускается. |
| `E2E_AUTH_EMAIL` | Email существующего тестового пользователя для второго сценария `e2e/auth.spec.ts` (только с `E2E_AUTH=1`). |
| `E2E_AUTH_PASSWORD` | Пароль того же тестового пользователя. |
| `RESEND_API_KEY` | Ключ Resend для подписки. Серверная, в клиентский бандл не попадает. Без неё форма подписки работает, но честно отвечает, что почта не подключена. |
| `RESEND_AUDIENCE_ID` | Идентификатор аудитории Resend, куда добавляются контакты. Нужен вместе с ключом. |
| `NEXT_PUBLIC_AMAZON_TAG` | Партнёрский тег Amazon Associates. Без неё блоки рендерятся, ссылки идут без тега. |
| `NEXT_PUBLIC_SITE_ORIGIN` | Корневой домен лендинга, по умолчанию `https://endgrain.app`. |
| `NEXT_PUBLIC_APP_ORIGIN` | Домен студии, по умолчанию `https://app.endgrain.app`. Нужна только для ссылок с лендинга и адресов в письмах Supabase; на домен cookie не влияет, его считает хост запроса. |
| `NEXT_PUBLIC_GA_MEASUREMENT_ID` | Id счётчика GA4 (`G-...`). Публичная. Без неё `<Analytics/>` не рендерит ни одного тега и не уходит ни одного запроса к googletagmanager.com - штатное состояние в CI и до заведения аналитики на проде. |

Значения переменных нигде не коммитятся, `.env.local` в `.gitignore`.

## Монетизация

Pro стоит $7 в месяц или $49 в год, оплата через Stripe Checkout (hosted, карточных данных мы не видим). Без ключей Stripe **Pro открыт всем**: `getProStatus()` отдаёт `pro: true` с причиной `no-stripe`, кнопки оплаты не рендерятся, а страница `/pricing` показывает честную строку «оплата пока не подключена». Paywall без работающей кассы это тупик, поэтому его нет.

Гейтов ровно три, и все они снимаются подпиской:

- лимит в 3 облачных проекта (единственный серверный гейт, проверяется в `saveProjectAction`);
- PNG для печати x4 до 4000 px (мягкий гейт, растеризация в браузере);
- промо-строка в подвале PDF-инструкции (мягкий гейт, PDF собирается в браузере).

Локальное сохранение, ссылка-шара, 3D, генераторы, photo-to-pattern, диагностика, расчёт себестоимости и сама PDF-инструкция бесплатны и не гейтятся никогда. Аварийный рубильник `NEXT_PUBLIC_PRO_UNLOCK=1` открывает Pro поверх всего, включая настроенный Stripe.

Вебхук живёт на `POST /api/stripe/webhook` и обрабатывает три события: `customer.subscription.created`, `.updated`, `.deleted`. Подпись проверяется своим кодом на `node:crypto`, SDK `stripe` в зависимостях нет.

## Домены

Один проект Next обслуживает два хоста, разведённых по заголовку Host в `proxy.ts`:

| Хост | Путь | Поведение |
| --- | --- | --- |
| `endgrain.app`, `www.endgrain.app` | `/` | рендерит лендинг (rewrite на `/landing`), без обращения к Supabase |
| `endgrain.app`, `www.endgrain.app` | `/landing` | canonical-путь лендинга, пропускается как есть |
| `endgrain.app`, `www.endgrain.app` | любой другой | редирект 307 на `app.endgrain.app` с тем же путём |
| `app.endgrain.app` | `/landing` | редирект 308 на `endgrain.app/landing`, чтобы не было двух индексируемых копий |
| `app.endgrain.app` | всё остальное | студия, как раньше |
| незнакомый хост (`localhost`, `127.0.0.1`, превью `*.vercel.app`) | всё | студия, лендинг доступен по `/landing` |

### Вход с лендинга и домен auth-cookie

Кнопки «Начать» на лендинге открывают вход модальным окном (`components/landing/AuthCta.tsx`), а не уводят на отдельную страницу. Сами страницы `/login`, `/register`, `/forgot-password` живы и остаются запасным входом: триггер модалки это настоящая ссылка на `app.endgrain.app/register`, поэтому без JavaScript, при провале гидратации и при клике с Cmd или Ctrl человек попадает на страницу.

Модалка живёт на `endgrain.app`, а студия на `app.endgrain.app`, и host-only cookie сессии на соседний домен браузер не отправит. Поэтому auth-cookie выписывается на регистрируемый домен `.endgrain.app` (`lib/supabase/cookies.ts`, домен считает `lib/routing/cookieDomain.ts`).

Домен считается от реального хоста в момент работы, а не от константы сборки: в браузере это `window.location.host`, на сервере и в `proxy.ts` заголовок `Host` запроса. Поэтому на localhost, по IP и на превью `*.vercel.app` домен не ставится вовсе, cookie остаётся host-only и вход работает; переменные окружения на это не влияют, выставлять `NEXT_PUBLIC_APP_ORIGIN` ради cookie не нужно. Тот же домен чинит PKCE: `-code-verifier`, записанный на лендинге при входе через Google, читает серверный `exchangeCodeForSession` на домене приложения. По той же схеме считается домен cookie с языком (`eg-locale`): серверный экшен и клиентский `rememberLocale` берут хост запроса и вкладки соответственно.

Что из этого следует:

- Опции cookie обязаны быть одинаковыми у всех трёх клиентов Supabase (`lib/supabase/browser.ts`, `server.ts`, `proxy.ts`), поэтому все трое зовут одну и ту же `supabaseCookieOptions(host)` от хоста запроса. Разъехавшись, они дадут две cookie с одним именем, и вход начнёт срабатывать через раз.
- Имя cookie задано явно (`sb-egs-auth`). При первом выкате старые cookie с дефолтным именем осиротеют, и все текущие сессии один раз попросят войти заново.
- Auth-cookie видит любой поддомен `endgrain.app`. Значит, на поддоменах нельзя размещать чужой или пользовательский контент, а на лендинг нельзя тащить сторонние скрипты.
- Настройки Supabase менять не нужно: адреса для писем и OAuth модалка строит от `APP_ORIGIN`, а не от текущего origin.

План Б, если приложение когда-нибудь уедет на другой регистрируемый домен (тогда общий cookie-домен невозможен): handoff по фрагменту. Модалка входит с хранилищем сессии в памяти, получает refresh-токен, уходит на `https://app.<домен>/auth/handoff#rt=...`, клиентская страница читает фрагмент (в Referer и на сервер он не попадает), делает `setSession`, чистит хеш и уводит в `next`. Кодом не написан.

Проверка, которую нельзя закрыть тестом (localhost не воспроизводит два origin): на превью с боевыми доменами войти через модалку на `endgrain.app`, убедиться, что студия на `app.endgrain.app` открылась уже авторизованной, а в DevTools у auth-cookie стоит домен `.endgrain.app` и дубля с host-only доменом нет.
